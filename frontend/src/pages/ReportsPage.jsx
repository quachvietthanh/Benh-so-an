import React,{useEffect,useState}from'react';import{Button,Card,DatePicker,message,Space,Table}from'antd';import dayjs from'dayjs';import reportApi from'../api/reportApi';const{RangePicker}=DatePicker
function ReportsPage(){const[range,setRange]=useState([dayjs().startOf('month'),dayjs()]),[summary,setSummary]=useState({}),[timeline,setTimeline]=useState([]),[medicines,setMedicines]=useState([]),[logs,setLogs]=useState([]);const params=()=>({from:range[0].format('YYYY-MM-DD'),to:range[1].format('YYYY-MM-DD')});const load=async()=>{try{const[s,t,m,a]=await Promise.all([reportApi.summary(params()),reportApi.timeline(params()),reportApi.topMedicines(params()),reportApi.audit(params())]);setSummary(s.data);setTimeline(t.data);setMedicines(m.data);setLogs(a.data)}catch(e){message.error(e.response?.data?.message||'Không thể tải báo cáo')}};useEffect(()=>{load()},[range]);const exportFile=async()=>{try{const r=await reportApi.export(params()),url=URL.createObjectURL(r.data),a=document.createElement('a');a.href=url;a.download=`bao-cao-${params().from}-${params().to}.csv`;a.click();URL.revokeObjectURL(url)}catch{message.error('Không thể xuất báo cáo')}};return <div><div className="page-header"><h2>Báo cáo vận hành</h2><Space><RangePicker value={range} onChange={v=>v&&setRange(v)}/><Button onClick={exportFile}>Xuất báo cáo</Button></Space></div><Space size="large" style={{marginBottom:16}}><Card title="Lượt khám"><h1>{summary.visitCount||0}</h1></Card><Card title="Doanh thu"><h1>{Number(summary.revenue||0).toLocaleString('vi-VN')} ₫</h1></Card></Space><Card title="Lượt khám theo ngày"><Table rowKey="reportDate" pagination={false} dataSource={timeline} columns={[{title:'Ngày',dataIndex:'reportDate'},{title:'Số lượt',dataIndex:'visitCount'}]}/></Card><Card title="Thuốc dùng nhiều" style={{marginTop:16}}><Table rowKey="name" pagination={false} dataSource={medicines} columns={[{title:'Thuốc',dataIndex:'name'},{title:'Số lượng cấp',dataIndex:'dispensedQuantity'}]}/></Card><Card title="Nhật ký truy cập bệnh án" style={{marginTop:16}}><Table rowKey="id" dataSource={logs} columns={[{title:'Người dùng',dataIndex:'userName'},{title:'Bệnh nhân',dataIndex:'patientName'},{title:'Bệnh án',dataIndex:'recordCode'},{title:'Hành động',dataIndex:'action'},{title:'Thời gian',dataIndex:'accessedAt'}]}/></Card></div>}
export default ReportsPage
